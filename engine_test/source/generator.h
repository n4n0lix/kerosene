#pragma once

#include <string>
        using std::string;

#include <chrono>
        using namespace std::chrono;
        using std::chrono::high_resolution_clock;

#include <iostream>
        using std::cout;
        using std::endl;

template<T>
class Generator<T>
{
public:
    
};